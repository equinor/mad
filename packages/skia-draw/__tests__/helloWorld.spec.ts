describe("Hello world", () => {
    it("is a hello world test to make sure the 'yarn test:all' passes. No other test is implemented yet. When you have started implementing tests in this package, you can delete this", () => {
        expect(true).toBe(true);
    });
});
