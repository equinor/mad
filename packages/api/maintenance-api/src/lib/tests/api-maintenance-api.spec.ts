import { apiMaintenanceApi } from './api-maintenance-api';

describe('apiMaintenanceApi', () => {
  it('should work', () => {
    expect(apiMaintenanceApi()).toEqual('api-maintenance-api');
  });
});
